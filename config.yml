env: "dev"
api_key: "${API_KEY}"

postgres:
  host: "postgres"
  port: "5432"
  username: "postgres"
  password: "${POSTGRES_PASSWORD}"
  database: "dictionary"

brokers:
  address: "kafka"
  port: "9092"
  topic: "words"